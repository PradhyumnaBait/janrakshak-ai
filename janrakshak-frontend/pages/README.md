# Legacy `pages` directory

This project uses the Next.js App Router (`/app` directory) for all routes.

The `/pages` folder is kept only to satisfy the requested structure and can
remain empty, or be used later for special cases if needed.

